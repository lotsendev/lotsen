root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/orchestrator ./cmd/orchestrator"
  bin = "./tmp/orchestrator"
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
