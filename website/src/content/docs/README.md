# Website docs content

Documentation pages for the website live in this directory as Markdown files.

- Add or edit files in `website/src/content/docs/*.md`.
- Keep filenames aligned with route slugs (for example, `getting-started.md` -> `/docs/getting-started`).
- The React docs pages load these files via `?raw` imports and render them with `react-markdown`.
